[alias]
	br=branch
	cm=commit
	cma=commit --amend
	co=checkout
	cp=cherry-pick
	st=status
	sw=switch
	rs=restore
	rst=checkout HEAD --
	tree=log --graph --full-history --all --pretty=format:"%h%x09%d%x20%s"
[push]
	default = simple

